"""""
There are 14  inbuilt data types:
int, float,complex, bool, str, bytes, bytearray, range, list, tuple, set, frozenset, dict, None

 & int, float,complex, bool, str are fundamental data types in python
"""
a = 10
#  to check  the type of  variable
print(type(a))
# to get the address of object
print(id(a))
